# הדרך הקלה — מערכת הזמנות שולחנית למסעדה

מערכת הזמנות בזמן אמת למסעדה: כל סועד סורק QR/NFC של השולחן ופותח את התפריט
בנייד שלו, כל מה שמוסיפים לעגלה מסונכרן בזמן אמת אל "טאבלט המאסטר" שעל
השולחן, ורק דרך הטאבלט הזה אפשר לשלוח מנות בפועל למטבח. יש תמיכה בפייר
(שליחה מתוזמנת של מנות לפי קורס — ראשונות/עיקריות/קינוחים) ובביטול מנה כל
עוד המטבח לא התחיל להכין אותה.

## פרסום באינטרנט (כדי לשלוח קישור אמיתי למסעדה/בר)

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/dvoraharbi0-pixel/the-easy-way)

לוחצים על הכפתור, מתחברים עם GitHub (אם עוד אין חשבון Render — נרשמים דרך
"Sign up with GitHub", זה חינמי), ומאשרים גישה לריפו. Render יזהה אוטומטית
את `render.yaml` שבשורש הפרויקט ויבנה שירות בשם `the-easy-way`. אחרי כ־1-2
דקות מקבלים כתובת קבועה כמו `https://the-easy-way-xxxx.onrender.com` —
זה הקישור לשלוח למסעדה, בלי תלות בדפדפן/מחשב שלכם.

**חשוב לדעת (תוכנית Free):**
- השירות "נרדם" אחרי כ-15 דקות בלי תנועה, וההפעלה הראשונה אחרי שינה
  לוקחת כ-30-50 שניות. לפני הדגמה חיה — כדאי לפתוח את הקישור דקה מראש
  כדי "להעיר" אותו.
- הנתונים (שולחנות, הזמנות) נשמרים בקובץ מקומי על השרת, אבל בתוכנית
  Free **אין דיסק קבוע** — כשהשירות נרדם ומתעורר מחדש, או כשעושים דיפלוי
  חדש, הנתונים מתאפסים ונוצרים מחדש (כולל טוקנים חדשים ל-QR של כל
  שולחן). בשביל הדגמות זה בסדר גמור; בשביל שולחנות עם QR מודפס שצריך
  להישאר תקף לאורך זמן, יהיה צריך בהמשך שדרוג לדיסק קבוע או מסד נתונים
  אמיתי.

## קוד צוות (הרשאה למסכי הצוות)

כל מסכי הצוות — טאבלט המאסטר, המטבח, הבר, ושני מסכי המלצרים — מוגנים בקוד
PIN משותף אחד. מי שנכנס אליהם רואה מסך "🔒 כניסת צוות" ולא יכול לעשות כלום
בלי הקוד; **הסועדים לא מושפעים בכלל** — התפריט (`/t/:token`) פתוח כרגיל בלי
קוד. ברירת המחדל היא `1234` — **חשוב להחליף אותה** לפני שנותנים למישהו אמיתי
את הקישור:

- **ב-Render**: בדף השירות → Environment → Add Environment Variable → מפתח
  `STAFF_PIN`, ערך לבחירתכם (למשל `7391`) → Save. השירות יתפרס מחדש
  אוטומטית עם הקוד החדש.
- **בהרצה מקומית**: `STAFF_PIN=7391 npm start`.

הקוד עצמו נשמר רק בזיכרון (כמו שאר הנתונים) — אחרי שינה/דיפלוי מחדש, אנשי
הצוות פשוט יזינו אותו שוב בפעם הבאה שהם נכנסים.

## הרצה

```bash
npm install
npm start
```

השרת עולה על `http://localhost:3000`.

- `/` — דף בית עם רשימת השולחנות, קוד QR לכל שולחן, וקישור ישיר לטאבלט המאסטר.
- `/t/:token` — תפריט הסועד (מה שנפתח לאחר סריקת ה-QR/NFC של השולחן).
- `/master/:token` — טאבלט המאסטר של השולחן.
- `/kitchen` — עמדת המטבח (כל המנות — ראשונות/עיקריות/קינוחים — מכל השולחנות).
- `/bar` — עמדת הבר (רק משקאות, מכל השולחנות).
- `/waiters` — מסך המלצרים של המטבח (מנות שסומנו כמוכנות, עם התראה קולית).
- `/waiters/bar` — מסך המלצרים של הבר (משקאות שסומנו כמוכנים, עם התראה קולית).

בסביבת פיתוח בלי מדפסות NFC/QR פיזיות — פותחים את `/` ולוחצים על הקישורים של
כל שולחן בכמה טאבים כדי לדמות מספר מכשירים.

## עריכת התפריט ותמונות

כל התפריט יושב בקובץ אחד — `server/seed.js` — כרשימה פשוטה של אובייקטים
(`name`, `description`, `price`, `category`, `course`, ואופציונלית `image`).
כדי לשנות מנה, מחיר או תיאור: עורכים את השורה המתאימה בקובץ הזה, שומרים,
ומעלים ל-GitHub (Render יעשה דיפלוי מחדש אוטומטית). **חשוב**: התפריט נזרע
מחדש רק אם `state.menuItems` ריק — ברגע שהמערכת כבר "רצה" עם תפריט קיים,
עריכת `seed.js` לא תשנה כלום עד לאיפוס נתונים (שממילא קורה אוטומטית כל
שינה/דיפלוי ב-Render, כמו שמוסבר למעלה).

**תמונות למנות**: לכל פריט יש `imageSlug` (למשל `hummus`) במקום נתיב קבוע.
בכל בקשת `/api/menu`, השרת מחפש בתיקייה `public/images/menu/` קובץ בשם
`<imageSlug>.jpg` / `.jpeg` / `.png` / `.webp` — **כל סיומת עובדת**, אין
צורך להקפיד על סיומת מדויקת. אם לא נמצא קובץ מתאים, מוצג אוטומטית
placeholder מעוצב (גרדיאנט + אייקון) במקום תמונה שבורה — כך שאפשר להוסיף
תמונות בהדרגה בלי לשבור כלום.

כדי להוסיף תמונה: שומרים את הקובץ עם השם המתאים (כל סיומת תמונה תקינה),
ומעלים אותו ל-GitHub לתיקייה `public/images/menu/` דרך אתר github.com
(Add file → Upload files, על הענף `claude/restaurant-table-ordering-system-i8zkq7`).
Render יעשה דיפלוי מחדש אוטומטית תוך דקה-שתיים.

השמות (`imageSlug`) של כל 23 המנות בתפריט הנוכחי:

| קטגוריה | מנה | שם קובץ |
|---|---|---|
| שתייה | מים מינרלים | `water` |
| שתייה | לימונדה ביתית עם נענע | `lemonade` |
| שתייה | קולה / קולה זירו | `cola` |
| שתייה | תה קר פירותי | `iced-tea` |
| שתייה | בירה מהחבית | `beer` |
| שתייה | כוס יין בית | `wine` |
| ראשונות | חומוס עם פטה | `hummus` |
| ראשונות | קרפצ׳יו בקר | `beef-carpaccio` |
| ראשונות | חסה גרילדת | `grilled-lettuce` |
| ראשונות | טרטר טונה אדומה | `tuna-tartare` |
| ראשונות | קרפצ׳יו סלק צלוי | `beet-carpaccio` |
| ראשונות | מרק העונה של השף | `soup-of-the-day` |
| עיקריות | אנטריקוט 300 גרם | `entrecote` |
| עיקריות | סלמון בגריל | `salmon` |
| עיקריות | ריזוטו פטריות יער | `mushroom-risotto` |
| עיקריות | פנה ארביאטה חריפה | `penne-arrabbiata` |
| עיקריות | דניס שלם בתנור | `baked-dorade` |
| עיקריות | המבורגר בית 200 גרם | `burger` |
| קינוחים | טירמיסו קלאסי | `tiramisu` |
| קינוחים | מלבי | `malabi` |
| קינוחים | פאדנט שוקולד חם | `chocolate-fondant` |
| קינוחים | קרם ברולה | `creme-brulee` |
| קינוחים | סורבה פירות העונה | `sorbet` |

## ארכיטקטורה

- **Backend**: Node.js + Express + Socket.io. State מוחזק בזיכרון (`server/store.js`)
  ונשמר ל-`data/store.json` בכל שינוי, כך שהפעלה מחדש של השרת לא מאבדת הזמנות פתוחות.
- **מודל נתונים** (`server/models.js`): שולחנות (`tables`), פריטי תפריט
  (`menuItems`), הזמנה פתוחה לשולחן (`sessions` — נוצרת אוטומטית בסריקה
  הראשונה ונסגרת ע"י הטאבלט המאסטר), סועדים (`diners`), ופריטי הזמנה
  (`orderItems`) עם מכונת מצבים:
  `in_cart → sent → preparing → ready → served`, וכן `cancelled` (רק
  מ-`sent`).
- **זמן אמת**: כל הצדדים (נייד סועד / טאבלט מאסטר / מסך מטבח) מחוברים ב-
  Socket.io ל"חדר" של ה-session; המטבח גם מחובר לחדר `kitchen` גלובלי שמרכז
  את כל השולחנות. כל שינוי במצב משדר מחדש את המצב המלא לכל הצדדים — מציאה
  פשוטה וחסינת-שגיאות למערכת בקנה מידה של מסעדה בודדת.
- **שתי עמדות הכנה**: כל פריט תפריט משויך לעמדה לפי הקורס שלו —
  `COURSE_STATION` ב-`public/shared/labels.js` ממפה `drink → bar` ואת
  `starter/main/dessert → kitchen`. עמדות המטבח/בר ומסכי המלצרים שלהן
  מאזינים לאותו broadcast (`kitchen:state`) ומסננים בצד הלקוח לפי המיפוי
  הזה — כך שאין צורך בחיבורי socket נפרדים, רק ב-HTML שונה לכל עמדה.
- **Frontend**: HTML/CSS/JS פשוטים (ללא build step), RTL מלא. חוויות נפרדות
  תחת `public/diner`, `public/master`, `public/kitchen` (גם משמש את `/bar`),
  `public/waiters` (גם משמש את `/waiters/bar`), ודף בית תחת `public/home`.

## זרימת השימוש

1. סועד סורק QR/NFC של השולחן → נפתח `/t/:token` → אם זו הפעם הראשונה
   שסורקים את השולחן, נוצרת session חדשה ("הזמנה פתוחה") אוטומטית.
2. הסועד ממלא שם ומתווסף כ-diner ל-session. הזהות נשמרת ב-localStorage כך
   שרענון הדף לא יוצר סועד כפול.
3. הסועד מוסיף מנות לעגלה — הפריט נכנס במצב `in_cart` ומופיע בזמן אמת גם
   אצל שאר הסועדים בשולחן וגם בטאבלט המאסטר.
   - **הערה למנה** (רשות, למשל אלרגנים): כפתור "📝 הוספת הערה" בכל פריט
     תפריט פותח שדה טקסט חופשי שנשמר על הפריט. ההערה מוצגת בכל מקום שהמנה
     מופיעה — עגלת הסועד, הטאבלט המאסטר, ובעיקר במסך המטבח/בר, שם היא
     מודגשת באדום כדי שלא תפספס.
   - **הסרה מהעגלה** (לפני שנשלח למטבח): גם הסועד שהוסיף (מהנייד שלו) וגם
     הטאבלט המאסטר יכולים להסיר פריט מה-`in_cart`. זו הסרה "רכה" — הפריט
     עובר למצב `removed` ולא נעלם, כך שהוא ממשיך להופיע בסעיף "הוסרו
     מהעגלה" גם בטאבלט המאסטר וגם בנייד של מי שהוסיף אותו במקור (גם אם
     המאסטר, ולא הסועד, הוא זה שהסיר). כפתור "↩️ החזרה לעגלה" זמין באותו
     סעיף גם בטאבלט המאסטר וגם בנייד — לוחצים ומחזירים את הפריט בדיוק
     כמות שהיה למצב `in_cart`.
4. **רק** הטאבלט המאסטר יכול לשלוח מנות בפועל למטבח:
   - שליחה בודדת של פריט.
   - שליחה מרובה של פריטים מסומנים.
   - **פייר לפי קורס** — כפתור "🔥 שלח את כל ה[קורס]" ששולח בבת אחת את כל
     פריטי ה-`in_cart` מאותו קורס (למשל: לשלוח את כל הראשונות עכשיו, ואת
     העיקריות רק כשרוצים — כדי לתזמן שהעיקריות יגיעו אחרי הראשונות).
5. מסך המטבח מציג את כל השולחנות עם המנות שנשלחו. הטבח מסמן "התחלת הכנה"
   (`preparing`) ואז "מוכן" (`ready`).
6. **ביטול מנה**: כפתור הביטול בטאבלט המאסטר מוצג אך ורק כשהמנה עדיין
   במצב `sent` (נשלחה אך המטבח לא התחיל להכין). ברגע שהטבח לוחץ "התחלת
   הכנה" הכפתור נעלם — לא ניתן לבטל מנה שכבר בהכנה.
7. **מסך המלצרים** (`/waiters`) מרכז מכל השולחנות רק מנות שסומנו `ready` —
   עם מספר שולחן בולט, זמן המתנה (מודגש באדום אחרי 3 דקות), והתראה קולית
   (מבוססת Web Audio, בלי קובץ שמע חיצוני) כשמנה חדשה מוכנה. עד שהמלצר לא
   לוחץ שם "✅ לקחתי, בדרך לשולחן" — גם הטאבלט המאסטר וגם נייד הסועד עדיין
   מציגים את המנה כ"בהכנה" ולא חושפים שהיא כבר מוכנה. ברגע שהמלצר לוחץ,
   המנה עוברת ל-`served`, והטאבלט המאסטר ("🍽️ הוגש — בתאבון!") ונייד
   הסועד ("🍽️ בתאבון!") מתעדכנים מיידית.
   - בנייד הסועד, שלושת המצבים `sent`/`preparing`/`ready` תמיד מוצגים
     ביחד כ"בהכנה" עם הודעה מתחלפת (👨‍🍳🔥✨) כל כמה שניות — כדי שהסועד לא
     "יידע" שהמנה כבר מוכנה לפני שהיא בפועל בדרך אליו.
8. **פיצול חשבון וטיפ אישי**: בכרטיסיית "עגלת השולחן" בנייד שלו, כל סועד
   רואה כרטיס "החשבון שלי" — סכום המנות שהוא עצמו הזמין (לא כל השולחן),
   ובוחר טיפ באמצעות כפתורי אחוזים מהירים (10/12/15/20%) או סכום מותאם
   אישית בש"ח. הטאבלט המאסטר מציג במקביל "פירוט חשבון לפי סועד" — סכום,
   טיפ וסה"כ לכל אחד מהסועדים, וכן סה"כ לשולחן כולל כל הטיפים.
9. **קריאה למלצר**: כפתור "🔔 קריאה למלצר" קבוע בראש נייד הסועד, בלי קשר
   להזמנת מנות — פותח בחירה מהירה (מים / סכו"ם / חשבון / עזרה) או הודעה
   חופשית. הקריאה מופיעה מיידית בשני מסכי המלצרים (`/waiters` ו-
   `/waiters/bar` — קריאה לא שייכת לעמדה ספציפית אז שניהם רואים אותה) עם
   מספר שולחן, זמן המתנה, והתראה קולית נפרדת מזו של "מנה מוכנה" (טון
   שונה כדי שאפשר להבדיל ביניהן לפי האוזן). המלצר מסמן "✅ טופל" כדי לסגור.
10. בסיום הארוחה, הטאבלט המאסטר סוגר את השולחן ("סגירת שולחן") — כל הסועדים
    מקבלים הודעה שהשולחן נסגר, וסריקה הבאה של אותו QR תפתח session חדשה.

## הרחבות אפשריות

- אימות/PIN לטאבלט המאסטר כדי שלא כל אחד יוכל לפתוח אותו.
- הדפסת/שליחת חשבון בסיום.
- תשלום מהנייד.
- ריבוי מסעדות/סניפים.
